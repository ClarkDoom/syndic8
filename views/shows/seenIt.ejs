<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<h1>Seen It</h1>

<ul>
  <% shows.forEach(show => { %>
    <% if (show.showType === "seenIt") { %> 
      <li><%= show.name %> </li>
    <% } %> 
  <% }) %>  
</ul>

<%- include('../partials/footer') %>
