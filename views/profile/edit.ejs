<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<h1>Edit Profile</h1>


<% if (user?.profile.avatar) { %>
  <div>
    <img
    src="<%= user.profile.avatar %>"
    alt="<%= user.profile.name %> avatar"
    />
  </div>
  <% } %>

  <!-- placeholder - needs to pull username from profile of user logged in  -->
  <h3>@username</h1>

  <h3>About Me</h3>
  
  <label for="username-input">Username: </label>
  <input type="text" name="userName" id="username-input"><br>

  <label for="about-me-input">About Me: </label>
  <textarea id="about-me-input" name="aboutMe" id="about-me" cols="30" rows="10"></textarea>
  <br>
  <button type="submit">Save</button>

<%- include('../partials/footer') %>
